name: sharoni
description: A new Flutter healthcare application.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  flutter_riverpod: ^2.5.1
  fl_chart: ^0.68.0
  google_fonts: ^6.2.1
  intl: ^0.20.2
  font_awesome_flutter: ^10.7.0
  device_preview: ^1.2.0
  http: ^1.2.1
  flutter_local_notifications: ^20.1.0
  timezone: ^0.10.1
  flutter_timezone: ^5.0.1
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/env

